import { styled, Box } from '@mui/material';
import palette from '~/cdk/theme/palette';

export const ReportPageContainer = styled(Box)`
  width: 100%;
  height: 100%;
  position: relative;
  background: ${palette.gradientDark};
`;
